{% extends 'suivies/index.html.twig' %}

{% block title %}New Traitements{% endblock %}

{% block contenue %}
    <div class="container">
        <div class="text-center">
            <h1>Création nouveau Traitements</h1>
        </div>
        <hr>

        {{ include('traitements/_form.html.twig') }}
        <br>
        <a href="{{ path('traitements_index') }}" class="btn btn-primary">Retour</a>
    </div>
{% endblock %}
