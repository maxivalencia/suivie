{% extends 'suivies/index.html.twig' %}

{% block title %}Traitements index{% endblock %}

{% block contenue %}
    <div class="container">
        <div class="text-center">
            <h1>Traitements index</h1>
        </div>

        <table class="table">
            <thead>
                <tr>
                    <th>Id</th>
                    <th>Traitement</th>
                    <th>actions</th>
                </tr>
            </thead>
            <tbody>
            {% for traitement in traitements %}
                <tr>
                    <td>{{ traitement.id }}</td>
                    <td>{{ traitement.traitement }}</td>
                    <td>
                        <a href="{{ path('traitements_show', {'id': traitement.id}) }}" class="btn btn-warning"><i class="fa fa-book" aria-hidden="true"></i></a>
                        <a href="{{ path('traitements_edit', {'id': traitement.id}) }}" class="btn btn-danger"><i class="fas fa-pencil-alt" aria-hidden="true"></i></a>
                    </td>
                </tr>
            {% else %}
                <tr>
                    <td colspan="3">Aucun enregistrement trouvé</td>
                </tr>
            {% endfor %}
            </tbody>
        </table>

        <a href="{{ path('traitements_new') }}" class="btn btn-success">Créer une région</a>
    </div>
{% endblock %}
