{% extends 'suivies/index.html.twig' %}

{% block title %}Edit Traitements{% endblock %}

{% block contenue %}
    <div class="container">
        <div class="text-center">
            <h1>Edition Traitements</h1>
        </div>
        <br>
        {{ include('traitements/_form.html.twig', {'button_label': 'Modifier'}) }}
        <br>
        <a href="{{ path('traitements_index') }}" class="btn btn-primary">Retour</a>
        <br><br>
        {{ include('traitements/_delete_form.html.twig') }}
    </div>
{% endblock %}
