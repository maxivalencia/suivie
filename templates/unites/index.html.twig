{% extends 'base.html.twig' %}

{% block title %}Unites index{% endblock %}

{% block body %}
    <h1>Unites index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Unite</th>
                <th>Abreviation</th>
                <th>Localite</th>
                <th>Mail</th>
                <th>Phone</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for unite in unites %}
            <tr>
                <td>{{ unite.id }}</td>
                <td>{{ unite.unite }}</td>
                <td>{{ unite.abreviation }}</td>
                <td>{{ unite.localite }}</td>
                <td>{{ unite.mail }}</td>
                <td>{{ unite.phone }}</td>
                <td>
                    <a href="{{ path('unites_show', {'id': unite.id}) }}">show</a>
                    <a href="{{ path('unites_edit', {'id': unite.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="7">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('unites_new') }}">Create new</a>
{% endblock %}
