{% extends 'suivies/index.html.twig' %}

{% block title %}New Unites{% endblock %}

{% block contenue %}
    <div class="container">
        <div class="text-center">
            <h1>Création nouveau Unites</h1>
        </div>
        <hr>

        {{ include('unites/_form.html.twig') }}
        <br>
        <a href="{{ path('unites_index') }}" class="btn btn-primary">Retour</a>
    </div>
{% endblock %}
