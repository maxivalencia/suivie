{% extends 'suivies/index.html.twig' %}

{% block title %}TypeDossiers{% endblock %}

{% block contenue %}
    <div class="container">
        <div class="text-center">
            <h1>Type Dossiers</h1>
        </div>

        <table class="table">
            <tbody>
                <tr>
                    <th>Id</th>
                    <td>{{ type_dossier.id }}</td>
                </tr>
                <tr>
                    <th>Type</th>
                    <td>{{ type_dossier.type }}</td>
                </tr>
            </tbody>
        </table>
        <br>
        <a href="{{ path('type_dossiers_index') }}" class="btn btn-primary">Retour</a>
        <br><br>
        <a href="{{ path('type_dossiers_edit', {'id': type_dossier.id}) }}" class="btn btn-warning">Modifier</a>
        <br><br>
        {{ include('type_dossiers/_delete_form.html.twig') }}
    </div>
{% endblock %}
