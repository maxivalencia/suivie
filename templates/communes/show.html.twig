{% extends 'base.html.twig' %}

{% block title %}Communes{% endblock %}

{% block body %}
    <h1>Communes</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ commune.id }}</td>
            </tr>
            <tr>
                <th>Commune</th>
                <td>{{ commune.commune }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('communes_index') }}">back to list</a>

    <a href="{{ path('communes_edit', {'id': commune.id}) }}">edit</a>

    {{ include('communes/_delete_form.html.twig') }}
{% endblock %}
