{% extends 'suivies/index.html.twig' %}

{% block title %}Edit Communes{% endblock %}

{% block contenue %}
    <div class="container">
        <div class="text-center">
            <h1>Edition Communes</h1>
        </div>
        <br>
        {{ include('communes/_form.html.twig', {'button_label': 'Modifier'}) }}
        <br>
        <a href="{{ path('communes_index') }}" class="btn btn-primary">Retour</a>
        <br><br>
        {{ include('communes/_delete_form.html.twig') }}
    </div>
{% endblock %}
