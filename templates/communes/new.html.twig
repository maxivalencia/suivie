{% extends 'suivies/index.html.twig' %}

{% block title %}New Communes{% endblock %}

{% block contenue %}
    <div class="container">
        <div class="text-center">
            <h1>Création nouvelle Communes</h1>
        </div>
        <hr>

        {{ include('communes/_form.html.twig') }}
        <br>
        <a href="{{ path('communes_index') }}" class="btn btn-primary">Retour</a>
    </div>
{% endblock %}
