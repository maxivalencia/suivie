{% extends 'suivies/index.html.twig' %}

{% block title %}Provinces{% endblock %}

{% block contenue %}
    <div class="container">
        <div class="text-center">
            <h1>Provinces</h1>
        </div>

        <table class="table">
            <tbody>
                <tr>
                    <th>Id</th>
                    <td>{{ province.id }}</td>
                </tr>
                <tr>
                    <th>Province</th>
                    <td>{{ province.province }}</td>
                </tr>
            </tbody>
        </table>
        <br>
        <a href="{{ path('provinces_index') }}" class="btn btn-primary">Retour</a>
        <br><br>
        <a href="{{ path('provinces_edit', {'id': province.id}) }}" class="btn btn-warning">Modifier</a>
        <br><br>
        {{ include('provinces/_delete_form.html.twig') }}
    </div>
{% endblock %}
