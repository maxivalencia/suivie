{% extends 'suivies/index.html.twig' %}

{% block title %}New Provinces{% endblock %}

{% block contenue %}
    <div class="container">
        <div class="text-center">
            <h1>Création nouveau Provinces</h1>
        </div>
        <hr>
        
        {{ include('provinces/_form.html.twig') }}
        <br>
        <a href="{{ path('provinces_index') }}" class="btn btn-primary">Retour</a>
    </div>
{% endblock %}
