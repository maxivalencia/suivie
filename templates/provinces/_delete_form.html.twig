<form method="post" action="{{ path('provinces_delete', {'id': province.id}) }}" onsubmit="return confirm('Etes-vous sûre de vouloir supprimer cette province?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ province.id) }}">
    <button class="btn btn-danger">Supprimer</button>
</form>
