{% extends 'suivies/index.html.twig' %}

{% block title %}Edition Regions{% endblock %}

{% block contenue %}
    <div class="container">
        <div class="text-center">
            <h1>Edition Regions</h1>
        </div>
        <br>
        {{ include('regions/_form.html.twig', {'button_label': 'Modifier'}) }}
        <br>
        <a href="{{ path('regions_index') }}" class="btn btn-primary">Retour</a>
        <br><br>
        {{ include('regions/_delete_form.html.twig') }}
    </div>
{% endblock %}
