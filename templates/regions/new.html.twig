{% extends 'suivies/index.html.twig' %}

{% block title %}New Regions{% endblock %}

{% block contenue %}
    <div class="container">
        <div class="text-center">
            <h1>Création nouveau Région</h1>
        </div>
        <hr>

        {{ include('regions/_form.html.twig') }}
        <br>
        <a href="{{ path('regions_index') }}" class="btn btn-primary">Retour</a>
    </div>
{% endblock %}
