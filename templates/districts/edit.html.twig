{% extends 'base.html.twig' %}

{% block title %}Edit Districts{% endblock %}

{% block body %}
    <h1>Edit Districts</h1>

    {{ include('districts/_form.html.twig', {'button_label': 'Update'}) }}

    <a href="{{ path('districts_index') }}">back to list</a>

    {{ include('districts/_delete_form.html.twig') }}
{% endblock %}
