{% extends 'suivies/index.html.twig' %}

{% block title %}Edit Districts{% endblock %}

{% block contenue %}
    <div class="container">
        <div class="text-center">
            <h1>Edition Districts</h1>
        </div>
        <br>
        {{ include('districts/_form.html.twig', {'button_label': 'Modifier'}) }}
        <br>
        <a href="{{ path('districts_index') }}" class="btn btn-primary">Retour</a>
        <br><br>
        {{ include('districts/_delete_form.html.twig') }}
    </div>
{% endblock %}
