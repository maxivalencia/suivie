{% extends 'base.html.twig' %}

{% block title %}Districts{% endblock %}

{% block body %}
    <h1>Districts</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ district.id }}</td>
            </tr>
            <tr>
                <th>District</th>
                <td>{{ district.district }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('districts_index') }}">back to list</a>

    <a href="{{ path('districts_edit', {'id': district.id}) }}">edit</a>

    {{ include('districts/_delete_form.html.twig') }}
{% endblock %}
