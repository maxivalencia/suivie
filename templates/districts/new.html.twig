{% extends 'suivies/index.html.twig' %}

{% block title %}New Districts{% endblock %}

{% block contenue %}
    <div class="container">
        <div class="text-center">
            <h1>Création nouveau Districts</h1>
        </div>
        <hr>

        {{ include('districts/_form.html.twig') }}
        <br>
        <a href="{{ path('districts_index') }}" class="btn btn-primary">Retour</a>
    </div>
{% endblock %}
