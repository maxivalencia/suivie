<form method="post" action="{{ path('districts_delete', {'id': district.id}) }}" onsubmit="return confirm('Etes-vous sûre de vouloir supprimer cette district?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ district.id) }}">
    <button class="btn btn-danger">Supprimer</button>
</form>
