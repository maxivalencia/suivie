{% extends 'suivies/index.html.twig' %}

{% block title %}Edit Dossiers{% endblock %}

{% block contenue %}
    <div class="container">
        <div class="text-center">
            <h1>Edition Dossiers</h1>
        </div>
        <br>
        {{ include('dossiers/_form.html.twig', {'button_label': 'Modifier'}) }}
        <br>
        <a href="{{ path('dossiers_index') }}" class="btn btn-primary">Retour</a>
        <br><br>
        {{ include('dossiers/_delete_form.html.twig') }}
    </div>
{% endblock %}
